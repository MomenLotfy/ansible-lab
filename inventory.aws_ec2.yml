---
plugin: amazon.aws.aws_ec2

regions:
  - us-east-1

filters:
  instance-state-name: running
  tag:Project: ansible-lab

hostnames:
  - ip-address

compose:
  ansible_host: public_ip_address

groups:
  web: "'ansible-lab' in (tags.Project | default(''))"
